log: debug

ssh:
  port: 2222
  identities:
    - "id_ed25519"

http:
  port: 4200
  domain: http://localhost:4200
  database:
    dsn: postgres://sdump:sdump@localhost:3432/sdump?sslmode=disable
    log_queries: true

  prometheus:
    username: sdump
    password: sdump
    is_enabled: true
